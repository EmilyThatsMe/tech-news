    <article class="post">
      <div class="title">
        <h2>{{title}}</h2>
      </div>
      <div>
          <p>{{post_url}}</p>
      </div>
      <div class="meta">
        by {{user.username}} on
        {{created_at}}
        |
       <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
      </div>
    </article>